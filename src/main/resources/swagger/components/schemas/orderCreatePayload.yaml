type: object
description: Represents an order
title: OrderCreatePayload
properties:
  product:
    $ref: './productInput.yaml'
    description: Represents a product
  quantity:
    type: integer
    format: int32
    nullable: false
    minimum: 1
    maximum: 100
  users:
    type: array
    nullable: false
    items:
      $ref: './userInput.yaml'
required:
  - product
  - quantity
  - users
